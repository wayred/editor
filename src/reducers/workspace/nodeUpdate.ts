import {Workspace} from "../../types";
import {ComponentConfig} from "@wayred/core";

const nodeUpdate = (state: Workspace | null, node: ComponentConfig) => {
  return state;
}

export default nodeUpdate;